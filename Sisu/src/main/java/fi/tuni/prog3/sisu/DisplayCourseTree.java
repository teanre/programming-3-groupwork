/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package fi.tuni.prog3.sisu;

import javafx.scene.control.TreeItem;
import javafx.scene.control.TreeView;

/**
 *
 * @author jamik
 */
public class DisplayCourseTree {
    
    TreeView<String> display = new TreeView<>();
    /*
    DisplayCourseTree() {
        for(DegreeModule d : degreeProgrammesData) {
                if(d.getName().equals(chosen)) {
                    var studyTree = new getStudyTree(d.getName());
                    studyTree.getStudyTreeOf(d.getGroupId());     
                    var selected = studyTree.getProgramme();
                    TreeItem<String> root = new TreeItem<>(selected.getName());
                    for(var child : selected.getChildren()) {
                        root.getChildren().add(new TreeItem(child.getName()));                  
                    }
                    
                    TreeView<String> display = new TreeView<>();
                    display.setRoot(root);
                    leftVBox.getChildren().add(display);
    }
*/
    
} 
